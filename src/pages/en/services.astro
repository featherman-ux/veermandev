---
import BaseLayout from '../../layouts/BaseLayout.astro';
import HeroSection from '../../components/sections/HeroSection.astro';
import TypewriterText from '../../components/ui/TypewriterText.astro';
import { getLangFromUrl, useTranslations, useTranslatedPath } from '../../i18n/utils';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
const tp = useTranslatedPath(lang);

const pageTitle = t('services.page.title');
const pageDescription = t('services.page.description');
---

<BaseLayout title={pageTitle} description={pageDescription}>
  <!-- LAUNCH OFFER BANNER -->
  <section class="launch-banner py-3 px-4 bg-gradient-to-r from-orange-500 to-red-500 text-white text-center relative overflow-hidden">
    <div class="max-w-4xl mx-auto relative z-10">
      <p class="text-sm font-medium">
        🤝 <strong>Intro offer:</strong> €100 off your first project + a complimentary brand check.
      </p>
    </div>
    <div class="banner-shine absolute inset-0 bg-gradient-to-r from-transparent via-white/20 to-transparent transform -skew-x-12 animate-shine"></div>
  </section>

  <!-- INTRO SECTION -->

<HeroSection 
  subtitle="Digital growth starts here"
  description="Discover how our tailored websites and strategies make your business faster, more visible, and more profitable. We do more than build—we think along with you."
>
  <span class="typewriter-heading text-center">
    <span class="typewriter-heading__label">Together we build</span>
    <TypewriterText 
      words={["a bold brand", "steady online leads", "growth without the hassle"]}
      className="typewriter-heading__text text-primary"
      initialText="a bold brand"
    />
  </span>
</HeroSection>


  <!-- PERSONAL INTRO -->
  <section class="personal-intro py-12 px-4">
    <div class="max-w-4xl mx-auto text-center bg-background-light dark:bg-background-dark border border-primary/10 rounded-xl p-8 shadow-sm">
      <h3 class="text-3xl font-semibold mb-4">We build your next online move together</h3>
      <p class="text-lg opacity-80">
        We stay close to every client so you never get lost in technical jargon. The goal: a website you’re proud to share and a steady stream of customers who find you through it.
      </p>
    </div>
  </section>

  <!-- PACKAGE COMPARISON -->
  <section class="packages-section py-16 px-4 reveal-section">
    <div class="max-w-7xl mx-auto">
      <div class="text-center mb-16">
        <h2 class="font-heading font-bold text-4xl mb-4">Choose the <span class="text-primary">package</span> that fits your ambitions</h2>
        <p class="text-lg opacity-80">Limited launch: €100 off every package + your complete brand strategy included.</p>
      </div>
      
      <div class="packages-grid grid md:grid-cols-2 lg:grid-cols-3 gap-8 max-w-6xl mx-auto">
        <!-- Package 1: Foundation -->
        <div class="package-card subtle-card bg-card-light dark:bg-card-dark rounded-xl shadow-lg hover:shadow-xl transition-all duration-300 group cursor-pointer relative flex flex-col">

          <div class="card-bg absolute inset-0 bg-gradient-to-br from-primary/5 to-primary/10 opacity-0 group-hover:opacity-100 transition-opacity duration-300 rounded-xl"></div>
          
          <div class="relative z-10 p-8 flex flex-col flex-grow">
            <div class="package-header text-center mb-8">
              <div class="pricing-section mb-4">
                <div class="original-price text-lg text-gray-500 line-through">€349</div>
                <div class="package-price text-4xl font-bold text-primary">€249</div>
                <div class="savings text-sm text-green-600 font-medium">Save €100</div>
              </div>
              <h3 class="text-2xl font-semibold mb-2">Foundation</h3>
              <p class="text-sm opacity-60">Perfect for coaches, consultants, and service professionals</p>
            </div>
            
            <div class="package-features space-y-4 mb-8 flex-grow">
              <div class="feature flex items-start gap-3">
                <span class="text-green-500 text-lg flex-shrink-0 mt-0.5">✓</span>
                <span>A website that visitors instantly trust and understand</span>
              </div>
              <div class="feature flex items-start gap-3">
                <span class="text-green-500 text-lg flex-shrink-0 mt-0.5">✓</span>
                <span>Brand story and tone of voice crafted around you</span>
              </div>
              <div class="feature flex items-start gap-3">
                <span class="text-green-500 text-lg flex-shrink-0 mt-0.5">✓</span>
                <span>Lightning-fast load times (under one second)</span>
              </div>
              <div class="feature flex items-start gap-3">
                <span class="text-green-500 text-lg flex-shrink-0 mt-0.5">✓</span>
                <span>Google and social profiles configured the right way</span>
              </div>
              <div class="feature flex items-start gap-3">
                <span class="text-green-500 text-lg flex-shrink-0 mt-0.5">✓</span>
                <span>Looks sharp on every phone and desktop</span>
              </div>
              <div class="feature flex items-start gap-3">
                <span class="text-green-500 text-lg flex-shrink-0 mt-0.5">✓</span>
                <span><strong>Hosting & updates included forever</strong></span>
              </div>
            </div>
            
            <div class="example-info bg-blue-50 dark:bg-blue-900/20 p-4 rounded-lg mb-6">
              <p class="text-sm"><strong>Example:</strong> Trust-building service website</p>
              <p class="text-xs opacity-60 mt-1">Ideal for showcasing your work and converting visitors into calls</p>
            </div>
            
            <a href={tp('contact')} class="block text-center bg-primary text-black font-semibold py-4 px-6 rounded-lg hover:bg-primary/90 transition-all mt-auto">
              Book your free strategy call
            </a>
          </div>
        </div>
        
        <!-- Package 2: Growth (Most Popular) -->
        <div class="package-card subtle-card bg-card-light dark:bg-card-dark rounded-xl shadow-lg hover:shadow-xl transition-all duration-300 group cursor-pointer relative border-2 border-primary flex flex-col">
          <div class="popular-badge-container absolute -top-4 left-0 right-0 flex justify-center z-30">
            <div class="popular-badge bg-primary text-black px-5 py-2 rounded-full text-sm font-bold shadow-lg border-2 border-white dark:border-gray-800">
              ⭐ Most chosen
            </div>
          </div>

          <div class="card-bg absolute inset-0 bg-gradient-to-br from-primary/5 to-primary/10 opacity-0 group-hover:opacity-100 transition-opacity duration-300 rounded-xl"></div>
          
          <div class="relative z-10 p-8 pt-12 flex flex-col flex-grow">
            <div class="package-header text-center mb-8">
              <div class="pricing-section mb-4">
                <div class="original-price text-lg text-gray-500 line-through">€449</div>
                <div class="package-price text-4xl font-bold text-primary">€349</div>
                <div class="savings text-sm text-green-600 font-medium">Save €100</div>
              </div>
              <h3 class="text-2xl font-semibold mb-2">Growth</h3>
              <p class="text-sm opacity-60">Perfect for local businesses ready to dominate their area</p>
            </div>
            
            <div class="package-features space-y-4 mb-8 flex-grow">
              <div class="feature flex items-start gap-3">
                <span class="text-green-500 text-lg flex-shrink-0 mt-0.5">✓</span>
                <span>Everything from Foundation, plus:</span>
              </div>
              <div class="feature flex items-start gap-3">
                <span class="text-green-500 text-lg flex-shrink-0 mt-0.5">✓</span>
                <span>An expanded brand plan with concrete actions</span>
              </div>
              <div class="feature flex items-start gap-3">
                <span class="text-green-500 text-lg flex-shrink-0 mt-0.5">✓</span>
                <span>5-8 pages engineered to drive calls or sales</span>
              </div>
              <div class="feature flex items-start gap-3">
                <span class="text-green-500 text-lg flex-shrink-0 mt-0.5">✓</span>
                <span>Clear content tailored to your key customer groups</span>
              </div>
              <div class="feature flex items-start gap-3">
                <span class="text-green-500 text-lg flex-shrink-0 mt-0.5">✓</span>
                <span>Local SEO so people in your area can find you</span>
              </div>
              <div class="feature flex items-start gap-3">
                <span class="text-green-500 text-lg flex-shrink-0 mt-0.5">✓</span>
                <span>Blog or portfolio section to keep your stories flowing</span>
              </div>
            </div>
            
            <div class="example-info bg-green-50 dark:bg-green-900/20 p-4 rounded-lg mb-6">
              <p class="text-sm"><strong>Live example:</strong> waarisniels.nl (travel blog)</p>
              <p class="text-xs opacity-60 mt-1">Feature-rich site with blog, galleries, and interactive maps</p>
            </div>
            
            <a href={tp('contact')} class="block text-center bg-primary text-black font-semibold py-4 px-6 rounded-lg hover:bg-primary/90 transition-all mt-auto">
              Book your free strategy call
            </a>
          </div>
        </div>
        
        <!-- Package 3: Complete -->
        <div class="package-card subtle-card bg-card-light dark:bg-card-dark rounded-xl shadow-lg hover:shadow-xl transition-all duration-300 group cursor-pointer relative flex flex-col">

          <div class="card-bg absolute inset-0 bg-gradient-to-br from-primary/5 to-primary/10 opacity-0 group-hover:opacity-100 transition-opacity duration-300 rounded-xl"></div>
          
          <div class="relative z-10 p-8 flex flex-col flex-grow">
            <div class="package-header text-center mb-8">
              <div class="pricing-section mb-4">
                <div class="original-price text-lg text-gray-500 line-through">€549</div>
                <div class="package-price text-4xl font-bold text-primary">€449</div>
                <div class="savings text-sm text-green-600 font-medium">Save €100</div>
              </div>
              <h3 class="text-2xl font-semibold mb-2">Complete</h3>
              <p class="text-sm opacity-60">Perfect for businesses ready to sell online</p>
            </div>
            
            <div class="package-features space-y-4 mb-8 flex-grow">
              <div class="feature flex items-start gap-3">
                <span class="text-green-500 text-lg flex-shrink-0 mt-0.5">✓</span>
                <span>Everything from Growth, plus:</span>
              </div>
              <div class="feature flex items-start gap-3">
                <span class="text-green-500 text-lg flex-shrink-0 mt-0.5">✓</span>
                <span>Webshop that feels trustworthy for your customers</span>
              </div>
              <div class="feature flex items-start gap-3">
                <span class="text-green-500 text-lg flex-shrink-0 mt-0.5">✓</span>
                <span>Secure payments and a frictionless checkout</span>
              </div>
              <div class="feature flex items-start gap-3">
                <span class="text-green-500 text-lg flex-shrink-0 mt-0.5">✓</span>
                <span>Inventory and order flows you can monitor at a glance</span>
              </div>
              <div class="feature flex items-start gap-3">
                <span class="text-green-500 text-lg flex-shrink-0 mt-0.5">✓</span>
                <span>Repeat orders and loyalty journeys handled for you</span>
              </div>
              <div class="feature flex items-start gap-3">
                <span class="text-green-500 text-lg flex-shrink-0 mt-0.5">✓</span>
                <span>Automated emails and follow-up sequences</span>
              </div>
            </div>
            
            <div class="example-info bg-purple-50 dark:bg-purple-900/20 p-4 rounded-lg mb-6">
              <p class="text-sm"><strong>Example:</strong> vrmn.shop (coming soon)</p>
              <p class="text-xs opacity-60 mt-1">Complete e-commerce solution for running online sales</p>
            </div>
            
            <a href={tp('contact')} class="block text-center bg-primary text-black font-semibold py-4 px-6 rounded-lg hover:bg-primary/90 transition-all mt-auto">
              Book your free strategy call
            </a>
          </div>
        </div>
      </div>
      
      <!-- All packages include -->
      <div class="all-packages-include mt-16 text-center bg-blue-50 dark:bg-blue-900/20 p-6 rounded-lg">
        <h3 class="font-semibold mb-4">Every package includes all of this by default:</h3>
        <div class="grid md:grid-cols-5 gap-4 text-sm">
          <div>✓ Crystal-clear brand messaging</div>
          <div>✓ Action plan for your ideal customer</div>
          <div>✓ Increased online visibility</div>
          <div>✓ Your unique story translated into copy</div>
          <div>✓ A committed partner after launch</div>
        </div>
        <p class="text-xs opacity-60 mt-4">Launch offer: this full brand strategy normally adds €200+ in value.</p>
      </div>
    </div>
  </section>

  <!-- WHY NOT WORDPRESS -->
  <section class="why-not-wordpress py-16 px-4 bg-card-light dark:bg-card-dark reveal-section">
    <div class="max-w-4xl mx-auto">
      <div class="text-center mb-12">
        <h2 class="font-heading font-bold text-4xl mb-4">Why not just choose <span class="text-primary">WordPress</span>?</h2>
        <p class="text-lg opacity-80">These are the three biggest differences for small businesses</p>
      </div>
      
      <div class="comparison-cards grid md:grid-cols-3 gap-8">
        <div class="highlight-card bg-background-light dark:bg-background-dark p-8 rounded-lg border border-primary/10 flex flex-col">
          <h3 class="text-xl font-semibold mb-4">⏱️ Faster</h3>
          <p class="text-sm opacity-80 mb-2"><strong>Our websites:</strong> load in under one second thanks to modern static hosting.</p>
          <p class="text-sm opacity-70"><strong>Most WordPress themes:</strong> 4–8 seconds because of heavy themes and plugins.</p>
        </div>
        <div class="highlight-card bg-background-light dark:bg-background-dark p-8 rounded-lg border border-primary/10 flex flex-col">
          <h3 class="text-xl font-semibold mb-4">💸 Lower cost</h3>
          <p class="text-sm opacity-80 mb-2"><strong>Our websites:</strong> no monthly bills for hosting or maintenance.</p>
          <p class="text-sm opacity-70"><strong>Most WordPress builds:</strong> €20–50 per month on hosting, maintenance, and plugin licenses.</p>
        </div>
        <div class="highlight-card bg-background-light dark:bg-background-dark p-8 rounded-lg border border-primary/10 flex flex-col">
          <h3 class="text-xl font-semibold mb-4">🛡️ More secure</h3>
          <p class="text-sm opacity-80 mb-2"><strong>Our websites:</strong> no plugins that can break or get hacked.</p>
          <p class="text-sm opacity-70"><strong>Most WordPress sites:</strong> vulnerable to hacks and broken updates.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- HOW IT WORKS -->
  <section class="process-section py-16 px-4 reveal-section">
    <div class="max-w-4xl mx-auto text-center">
      <h2 class="font-heading font-bold text-4xl mb-12">How it <span class="text-primary">works</span></h2>
      
      <div class="process-steps grid md:grid-cols-3 gap-12">
        <div class="step group">
          <div class="step-number w-20 h-20 bg-primary text-black rounded-full flex items-center justify-center text-3xl font-bold mx-auto mb-6 group-hover:scale-110 transition-transform duration-300">1</div>
          <h3 class="text-xl font-semibold mb-4">Tell us what you need</h3>
          <p class="text-sm opacity-80">A quick call or message about your business. We recommend the right package and explain exactly how it works.</p>
        </div>
        
        <div class="step group">
          <div class="step-number w-20 h-20 bg-primary text-black rounded-full flex items-center justify-center text-3xl font-bold mx-auto mb-6 group-hover:scale-110 transition-transform duration-300">2</div>
          <h3 class="text-xl font-semibold mb-4">We build your website</h3>
          <p class="text-sm opacity-80"><strong>Just 3 days</strong> to design and develop. We share updates, ask for feedback, and keep everything upfront.</p>
        </div>
        
        <div class="step group">
          <div class="step-number w-20 h-20 bg-primary text-black rounded-full flex items-center justify-center text-3xl font-bold mx-auto mb-6 group-hover:scale-110 transition-transform duration-300">3</div>
          <h3 class="text-xl font-semibold mb-4">You launch and keep growing</h3>
          <p class="text-sm opacity-80">Your fast, professional site goes live with <strong>free hosting & updates forever</strong>. We show you how to keep things fresh.</p>
        </div>
      </div>
      
      <div class="process-guarantee mt-12 bg-yellow-50 dark:bg-yellow-900/20 p-6 rounded-lg border border-yellow-200 dark:border-yellow-800">
        <p class="font-semibold text-yellow-800 dark:text-yellow-300">
          🎯 <strong>Our promise:</strong> Fixed price, delivered in 3 days, lifetime hosting & updates. No surprises.
        </p>
      </div>
    </div>
  </section>

  <!-- FAQ SECTION -->
  <section class="faq-section py-16 px-4 bg-card-light dark:bg-card-dark reveal-section">
    <div class="max-w-4xl mx-auto">
      <div class="text-center mb-12">
        <h2 class="font-heading font-bold text-4xl mb-4">Frequently asked <span class="text-primary">questions</span></h2>
        <p class="text-lg opacity-80">Quick answers from fellow small business owners</p>
      </div>
      
      <div class="faq-list space-y-4">
        <div class="faq-item bg-background-light dark:bg-background-dark rounded-lg">
          <button class="faq-question w-full text-left p-6 font-semibold flex justify-between items-center hover:text-primary transition-colors duration-300">
            Can I update the website myself?
            <span class="faq-toggle text-2xl">+</span>
          </button>
          <div class="faq-answer hidden p-6 pt-0 opacity-80">
            <p>Absolutely. We teach you how to edit copy, add photos, and publish new content. No monthly fee for simple updates like changing your phone number or opening hours.</p>
          </div>
        </div>
        
        <div class="faq-item bg-background-light dark:bg-background-dark rounded-lg">
          <button class="faq-question w-full text-left p-6 font-semibold flex justify-between items-center hover:text-primary transition-colors duration-300">
            What if I need changes after launch?
            <span class="faq-toggle text-2xl">+</span>
          </button>
          <div class="faq-answer hidden p-6 pt-0 opacity-80">
            <p>Small tweaks and updates are included forever. For bigger changes—new pages, extra features—we charge transparent rates (usually €25-50). We’re always here to help.</p>
          </div>
        </div>
        
        <div class="faq-item bg-background-light dark:bg-background-dark rounded-lg">
          <button class="faq-question w-full text-left p-6 font-semibold flex justify-between items-center hover:text-primary transition-colors duration-300">
            Do you work with really small businesses?
            <span class="faq-toggle text-2xl">+</span>
          </button>
          <div class="faq-answer hidden p-6 pt-0 opacity-80">
            <p>Definitely. We specialise in local small businesses—from solo freelancers to family restaurants and small retailers. Helping you win more customers is what we do.</p>
          </div>
        </div>
        
        <div class="faq-item bg-background-light dark:bg-background-dark rounded-lg">
          <button class="faq-question w-full text-left p-6 font-semibold flex justify-between items-center hover:text-primary transition-colors duration-300">
            Is free hosting really enough?
            <span class="faq-toggle text-2xl">+</span>
          </button>
          <div class="faq-answer hidden p-6 pt-0 opacity-80">
            <p>Yes. Our websites are so efficient they barely use server resources. We can offer lifetime hosting because the modern stack we use is far leaner than WordPress.</p>
          </div>
        </div>
        
        <div class="faq-item bg-background-light dark:bg-background-dark rounded-lg">
          <button class="faq-question w-full text-left p-6 font-semibold flex justify-between items-center hover:text-primary transition-colors duration-300">
            What happens if you disappear?
            <span class="faq-toggle text-2xl">+</span>
          </button>
          <div class="faq-answer hidden p-6 pt-0 opacity-80">
            <p>Great question. You own the website files and we use standard hosting services. If anything happens, any developer can take over your site. You’re never locked in.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- FINAL CTA -->
  <section class="final-cta py-16 px-4 text-center">
    <div class="max-w-3xl mx-auto">
      <h2 class="font-heading font-bold text-4xl mb-6">Ready to win more <span class="text-primary">customers</span>?</h2>
      <p class="text-lg opacity-80 mb-8">
        Stop losing leads to a slow, outdated website. Let’s build something that actually works. Free hosting & lifetime updates are part of every project.
      </p>
      <div class="cta-buttons flex flex-col sm:flex-row gap-4 justify-center">
        <a
          href={tp('contact')}
          class="bg-primary text-black font-semibold px-8 py-4 rounded-lg text-xl hover:bg-primary/90 transition-all transform hover:scale-105 inline-block"
        >
          Book your free strategy call
        </a>
        <a
          href={tp('portfolio')}
          class="bg-card-light dark:bg-card-dark border-2 border-primary text-primary font-semibold px-8 py-4 rounded-lg text-xl hover:bg-primary hover:text-black transition-all inline-block"
        >
          See real examples
        </a>
      </div>
    </div>
  </section>

  <script>
    document.addEventListener('DOMContentLoaded', () => {
      // FAQ functionality
      const faqQuestions = document.querySelectorAll('.faq-question');
      
      faqQuestions.forEach((question) => {
        question.addEventListener('click', () => {
          const answer = question.nextElementSibling;
          if (!(answer instanceof HTMLElement)) return;

          const toggle = question.querySelector('.faq-toggle');
          if (!(toggle instanceof HTMLElement)) return;

          if (answer.classList.contains('hidden')) {
            answer.classList.remove('hidden');
            toggle.textContent = '−';
          } else {
            answer.classList.add('hidden');
            toggle.textContent = '+';
          }
        });
      });

      // Enhanced scroll reveals
      const observerOptions = {
        root: null,
        rootMargin: '-50px',
        threshold: 0.1
      };

      const observer = new IntersectionObserver((entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            const element = entry.target;

            if (!(element instanceof HTMLElement)) return;
            element.style.opacity = '1';
            element.style.transform = 'translateY(0) scale(1)';
            
            // Stagger children animation
            const children = element.querySelectorAll('.package-card, .step, .faq-item');
            children.forEach((child, childIndex) => {
              if (!(child instanceof HTMLElement)) return;

              const childElement = child;
              setTimeout(() => {
                childElement.style.opacity = '1';
                childElement.style.transform = 'translateY(0)';
              }, childIndex * 150);
            });
          }
        });
      }, observerOptions);

      // Apply to sections
      const sections = document.querySelectorAll('.reveal-section');
      sections.forEach(section => {
        if (!(section instanceof HTMLElement)) return;

        const element = section;
        element.style.opacity = '0';
        element.style.transform = 'translateY(30px) scale(0.98)';
        element.style.transition = 'opacity 0.8s cubic-bezier(0.16, 1, 0.3, 1), transform 0.8s cubic-bezier(0.16, 1, 0.3, 1)';
        observer.observe(element);
      });

      // Subtle card effects
      const subtleCards = document.querySelectorAll('.subtle-card');
      subtleCards.forEach(card => {
        if (!(card instanceof HTMLElement)) return;

        const cardElement = card;
        
        cardElement.addEventListener('mousemove', (e) => {
          const rect = cardElement.getBoundingClientRect();
          const x = e.clientX - rect.left;
          const y = e.clientY - rect.top;
          
          const centerX = rect.width / 2;
          const centerY = rect.height / 2;
          
          const rotateX = (y - centerY) / 80;
          const rotateY = (centerX - x) / 80;
          
          cardElement.style.transform = `perspective(1000px) rotateX(${rotateX}deg) rotateY(${rotateY}deg) translateZ(3px)`;
        });
        
        cardElement.addEventListener('mouseleave', () => {
          cardElement.style.transform = 'perspective(1000px) rotateX(0deg) rotateY(0deg) translateZ(0px)';
        });
      });
    });
  </script>

  <style>
    /* Launch banner animation */
    @keyframes shine {
      0% { transform: translateX(-100%) skewX(-12deg); }
      100% { transform: translateX(200%) skewX(-12deg); }
    }
    
    .animate-shine {
      animation: shine 3s ease-in-out infinite;
    }

    .subtle-card {
      transition: transform 0.3s cubic-bezier(0.16, 1, 0.3, 1), box-shadow 0.3s cubic-bezier(0.16, 1, 0.3, 1);
      transform-style: preserve-3d;
      min-height: 600px;
    }

    .subtle-card:hover {
      box-shadow: 0 8px 25px rgba(59, 130, 246, 0.15);
    }

    .popular-badge-container {
      z-index: 30;
    }
    
    .popular-badge {
      box-shadow: 0 6px 20px rgba(59, 130, 246, 0.5);
      white-space: nowrap;
      animation: badgeFloat 3s ease-in-out infinite;
    }

    @keyframes badgeFloat {
      0%, 100% { 
        transform: translateY(0) scale(1); 
        box-shadow: 0 6px 20px rgba(59, 130, 246, 0.5);
      }
      50% { 
        transform: translateY(-2px) scale(1.02); 
        box-shadow: 0 8px 25px rgba(59, 130, 246, 0.6);
      }
    }

    .launch-offer-badge {
      animation: pulse 2s ease-in-out infinite;
    }

    @keyframes pulse {
      0%, 100% { opacity: 1; transform: scale(1); }
      50% { opacity: 0.8; transform: scale(1.05); }
    }
    
    .packages-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
      gap: 2rem;
      align-items: stretch;
    }
    
    @media (max-width: 1024px) {
      .packages-grid {
        grid-template-columns: 1fr;
        max-width: 400px;
        margin: 0 auto;
      }
    }
    
    @media (min-width: 1024px) {
      .packages-grid {
        grid-template-columns: repeat(3, 1fr);
      }
    }

    .comparison-cards .highlight-card {
      min-height: 220px;
    }
    
    .faq-question:hover .faq-toggle {
      color: var(--color-primary);
    }

    .flex-shrink-0 {
      flex-shrink: 0;
    }
    
    .flex-grow {
      flex-grow: 1;
    }
  </style>
</BaseLayout>
