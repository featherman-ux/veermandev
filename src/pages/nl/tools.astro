---
import BaseLayout from '../../layouts/BaseLayout.astro';

const pageTitle = 'Gratis Website Tools | VeermanDevelopment';
const pageDescription = 'Lichte checks om snelheid, mobiele UX en basiszaken te beoordelen—zonder aanmelding.';
---

<BaseLayout title={pageTitle} description={pageDescription}>
  <section class="py-16 px-4">
    <div class="max-w-4xl mx-auto text-center mb-10">
      <h1 class="font-heading font-bold text-5xl mb-4">Gratis Website Tools</h1>
      <p class="text-lg opacity-80">Snel en no‑nonsense. Voor een diepere audit: neem contact op.</p>
    </div>

    <div class="max-w-3xl mx-auto grid gap-8">
      <div class="bg-card-light dark:bg-card-dark rounded-xl p-6 border border-gray-200 dark:border-gray-700">
        <h2 class="text-2xl font-semibold mb-2">Snelle Audit (Checklist)</h2>
        <p class="opacity-80 mb-4">Vul je URL in en volg de checklist. We halen je site hier niet op—privacy gewaarborgd.</p>
        <form onsubmit="return false;" class="space-y-4">
          <input id="qa-url-nl" type="url" placeholder="https://voorbeeld.nl" class="w-full p-3 rounded border border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-800" />
          <button id="qa-run-nl" class="bg-primary text-black font-semibold px-5 py-3 rounded">Genereer Checklist</button>
        </form>
        <div id="qa-results-nl" class="mt-4 text-sm opacity-90 hidden"></div>
      </div>
    </div>
  </section>

  <script>
    const btn = document.getElementById('qa-run-nl')!;
    const out = document.getElementById('qa-results-nl')!;
    btn.addEventListener('click', () => {
      out.classList.remove('hidden');
      out.innerHTML = `
        <div class="space-y-2">
          <div>□ Doeltijd laden: onder 2s op 4G</div>
          <div>□ Duidelijke, probleem‑gerichte headline</div>
          <div>□ Primaire CTA boven de vouw</div>
          <div>□ Mobiele tekst ≥ 16px; knoppen makkelijk te tikken</div>
          <div>□ NAP‑info + Google Bedrijfsprofiel gelinkt</div>
          <div>□ Alt‑tekst voor belangrijke afbeeldingen</div>
          <div>□ Basis schema (LocalBusiness)</div>
        </div>`;
    });
  </script>
</BaseLayout>

